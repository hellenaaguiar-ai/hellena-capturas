# Copie este arquivo para .env e preencha com sua chave real.
# .env NAO deve ser commitado.

# Chave da OpenAI (platform.openai.com). Usada para transcricao de reunioes
# e para enviar o TEXTO ja transcrito (sintese/classificacao) - o audio de
# ideia/terapia/aula/reflexao nunca e enviado por aqui, so passa pela
# transcricao local (Whisper).
OPENAI_API_KEY=sk-...
