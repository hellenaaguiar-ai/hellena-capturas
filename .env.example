# Copie este arquivo para .env e preencha com sua chave real.
# .env NAO deve ser commitado.

# Chave da Claude API (console.anthropic.com). Usada apenas para enviar o
# TEXTO ja transcrito (sintese/classificacao) - o audio nunca e enviado por
# aqui, so passa pela transcricao local (Whisper).
ANTHROPIC_API_KEY=sk-ant-...
